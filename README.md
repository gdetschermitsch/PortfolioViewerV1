# CRUXTAIN Portfolio — Per-Image Mature Reveal Patch

Manifest-only package. Media files are intentionally excluded.

## This patch
- Changes each locked image-card button to: `I am 18+- reveal this image`.
- Reveals only the specific mature image that was confirmed.
- Preserves the existing page-level censorship toggle behavior.
- Keeps the previous category fixes: stacked collapsible image/model groups and Sketches folder precedence.

## Install
Copy these files over the existing portfolio files, then place your media folders/files back into the expected paths referenced by `portfolio.json`.
